<%= form_with(model: illness_control, local: true) do |form| %>
  <% if illness_control.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(illness_control.errors.count, "error") %> prohibited this illness_control from being saved:</h2>

      <ul>
      <% illness_control.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
