<%= form_with(model: harvest_procedure, local: true) do |form| %>
  <% if harvest_procedure.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(harvest_procedure.errors.count, "error") %> prohibited this harvest_procedure from being saved:</h2>

      <ul>
      <% harvest_procedure.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
