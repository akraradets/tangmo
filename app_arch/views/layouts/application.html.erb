<!DOCTYPE html>
<html>
  <head>
    <!-- Required meta tags -->
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">
    <title>Tangmo</title>
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <link rel="shortcut icon" type="image/png" href="<%= image_url('TANGMO_logo.png') %>"/>
    <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_include_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>

<style>
  .bg-pattern {
    background-image: url("<%= image_url('TANGMO_pattern.png') %>");
    background-color: #d7ccc8 !important;
  }
  .my-container {
      /* background: #d7ccc8; */
      /* overflow: hidden; */
      background-repeat: 'repeat'
  }
  /* You could use :after - it doesn't really matter */
  .my-container:after {
      content: ' ';
      position: absolute;
      left: 0;
      top: 0;
      width: 100%;
      height: 100%;
      opacity: 0.1;
      background-image: url("<%= image_url('TANGMO_pattern.png') %>");
      /* background-position: 50% 0; */
  }
  p {
    margin-top: 0;
    margin-bottom: 1rem;
    font-size: 1rem;
  }
  ul {
    font-size: 1rem;
  }
  .btn-light-brown {
      /* color: #212529; */
      background-color: #d7ccc8;
      border-color: #d7ccc8;
  }
</style>
  <body>
    <script src="https://www.promisejs.org/polyfills/promise-7.0.4.min.js"></script>
    <script src="https://www.promisejs.org/polyfills/promise-done-7.0.4.min.js"></script>
    <script src="https://cdnjs.cloudflare.com/ajax/libs/waypoints/4.0.1/jquery.waypoints.min.js"></script>
    <!-- Nav bar -->
    <nav class="navbar fixed-top navbar-expand-md navbar-dark bg-main">
      <a class="navbar-brand" href="/"> <%= image_tag 'TANGMO_logo.png', height:37, width:37 %></a>

      <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
      </button>
      <!-- Navbar Left -->
      <div class="collapse navbar-collapse" id="navbarSupportedContent">
        <ul class="navbar-nav mr-auto">
          <li class="nav-item active">
            <a class="nav-link" href="<%= farmers_path %>">เกษตรกร</a>
          </li>
          <li class="nav-item active">
            <a class="nav-link" href="<%= plots_path %>">แปลงแตงโม</a>
          </li>
          <% if !current_user.nil? %>
          <li class="nav-item dropdown">
            <a class="nav-link dropdown-toggle" href="#" id="navbarDropdown" role="button" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
              หน้าจัดการ
            </a>
            <div class="dropdown-menu" aria-labelledby="navbarDropdown">
              <a class="dropdown-item" href="<%= new_farmer_path %>">เพิ่มเกษตรกร</a>
              <a class="dropdown-item" href="<%= new_plot_path %>">เพิ่มแปลงแตงโม</a>
            </div>
          </li>
          <% end %>
        </ul>
        <!-- Navbar Right -->
        <ul class="navbar-nav">
          <% if user_signed_in? %>
            <!-- d-none d-md-block => Show when on desktop-->   
            <li class="nav-item d-none d-md-block">
              <span class="navbar-text">ยินดีตอนรับ</span> 
              <span style="color:white;"><b><%= current_user.username %></b></span>
            </li>
            <li class="nav-item d-none d-md-block">
              <div class="dropdown">
                <a class="nav-link dropdown-toggle" href="#" id="navbarDropdown" role="button" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false"></a>
                <div class="dropdown-menu dropdown-menu-right" aria-labelledby="navbarDropdown">
                  <a class="dropdown-item" rel="nofollow" data-method="delete" href="<%= destroy_user_session_path %>"><i class="fas fa-sign-out-alt" style="margin-right: 5px;"></i>ออกจากระบบ</a>
              </div>
            </li>
            <!-- d-block d-md-none => Show when on phone-->
              <li class="nav-item d-block d-md-none">
                <a class="nav-link active" data-method="delete" href="<%= destroy_user_session_path %>">
                  <span class="fas fa-sign-out-alt" style="margin-right: 5px;"></span>ออกจากระบบ
                  <span><b><%= current_user.username %></b></span>
                </a>
              </li>
          <% else %>
            <li class="nav-item">
              <a class="btn btn-outline-light d-none d-md-block" href="<%= new_user_session_path %>">เข้าสู่ระบบ</a>
              <a class="nav-link d-block d-md-none" href="<%= new_user_session_path %>">เข้าสู่ระบบ</a>
            </li>
          <% end %>
        </ul>
      </div>

    </nav>
    <!-- Content -->
    <div class="container-fluid">
      <div class="row justify-content-center" style="margin-top:70px; margin-bottom:60px;">
        <div class="col-12 col-lg-8">
        <!-- Content -->
          <% if flash[:notice] %>
            <p class="alert alert-success"><%= flash[:notice] %></p>
          <% end %>
          <% if flash[:alert] %>
            <p class="alert alert-danger"><%= flash[:alert] %></p>
          <% end %>
          <br>
          <%= yield %>
        </div>
      </div>
    </div>
    <!-- Footer -->
    <nav class="navbar fixed-bottom navbar-dark bg-dark" style="color:white; display:none;">
      <div>
        <small>© Copyright PHANGAN ORGANIC COCONUT TRACABILITY SYSTEM.</small>
      </div>
      <div>
        <%# <small>Web Application Engineering</small> %>
      </div>
      <div>
        <small>Created By โครงการพัฒนามะพร้าวอินทรีย์เกาะพะงัน</small>
      </div>
    </nav>
  </body>
  </body>

</html>
